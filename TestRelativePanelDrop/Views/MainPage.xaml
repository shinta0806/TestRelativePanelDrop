<Page
    x:Class="TestRelativePanelDrop.Views.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Background="{ThemeResource SolidBackgroundFillColorBaseBrush}"
    mc:Ignorable="d">

	<StackPanel Margin="20" >
		<TextBlock Text="Drop a file in the box below." />
		<TextBlock Text="{x:Bind ViewModel.Path, Mode=OneWay}" />
		<RelativePanel Width="300" Height="300" 
					   AllowDrop="True" DragOver="{x:Bind ViewModel.DragOver}" Drop="{x:Bind ViewModel.Drop}" 
					   BorderThickness="1" BorderBrush="Black" />
	</StackPanel>
</Page>
